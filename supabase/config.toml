project_id = "elogbook"

[db]
port = 54322

[auth]
enabled = true
site_url = "http://localhost:3000"
additional_redirect_urls = ["http://localhost:3000/auth/callback", "elogbook://auth/callback", "http://localhost:8081/*"]
jwt_expiry = 3600
# P2.16: enterprise-grade auth — admin-invite-only (no self-signup),
# email confirmation required, timeboxed sessions.
enable_signup = false

[auth.email]
enable_confirmations = true
enable_autoconfirm = false
# SMTP must be configured at the Supabase project level. Local dev
# can use a local SMTP catcher (e.g. MailHog on :1025) or the
# inbucket included in the local Supabase stack.
# smtp_host = "<SMTP_HOST>"
# smtp_port = 587
# smtp_user = "<SMTP_USER>"
# smtp_pass = "<SMTP_PASS>"
# smtp_admin_email = "admin@elogbook.example"
# smtp_sender_name = "E-Logbook"

[auth.sessions]
# P2.16: timebox PHI access to 8h absolute, 30 min idle.
timebox = true
timebox_duration = 28800  # 8h
inactivity_timeout = 1800  # 30 min

[auth.rate_limit]
token_refresh = 30
signup = 5
login = 10

[auth.mfa]
# P6.1: TOTP MFA enabled; required for director+ at app level.
max_enrolled_factors = 5

[auth.sso]
# P6.0: enterprise SSO. Each tenant can register a SAML metadata URL or
# OIDC discovery URL via the `tenant_sso_configs` table (migration 00058).
# Supabase's built-in SSO is enabled here at the project level; the per-
# tenant routing lives in the `sso-callback` edge function.
enabled = true

[api]
enabled = true
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public"]

[edge_runtime]
enabled = true
policy = "per_worker"

[analytics]
enabled = false

[storage]
enabled = true
# P6.9: 20MB per-file limit for case attachments. Per-tenant quota
# (in MB) is stored on subscription_plans.storage_quota_mb and
# enforced by the case-attachments bucket policy in migration 00061.
file_size_limit = "20MB"

[functions]
enabled = true
verify_jwt = true
import_map = "./import_map.json"

[functions.generate-pdf]
[functions.ai-insights]
[functions.payment-webhook]
[functions.create-checkout]
[functions.sso-callback]
[functions.dispatch-webhook]
