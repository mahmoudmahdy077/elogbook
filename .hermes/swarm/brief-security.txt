SECURITY+RBAC MISSION (task_e043afe0a195). Repo G:\elogbook. Creds from .env.local (NEXT_PUBLIC_SUPABASE_URL/NEXT_PUBLIC_SUPABASE_ANON_KEY). Demo accounts password 'password123!': resident@demo.com supervisor@demo.com director@demo.com admin@demo.com platform@demo.com. Tenant 9cd50d60-febe-4adf-be0f-a36bf82762f6.
Login as each role and verify: cross-tenant reads/writes on case_entries/profiles/notifications/tenant_webhooks vs foreign tenant 00000000-0000-0000-0000-000000000001 must fail or return empty; PATCH own profiles.role=director rejected; approve_case RPC as resident rejected; invoice/billing endpoints as non-admin rejected; anon-key unauthenticated reads of case_entries/profiles/audit_logs/notifications/tenant_webhooks return zero rows; audit_logs scoping (resident sees own only); forged client INSERT into audit_logs rejected. Pace logins ~3s apart; retry once after 15s on rate-limit errors. Create NO rows except where a negative test needs one (clean up after).
WRITE your full matrix (check / expected / actual / PASS-FAIL / severity P1-P3) to .hermes/swarm/security-rbac.md then reply exactly: SWARM-DONE security-rbac
